var data = [60, 90, 80, 100, 30, 20, "fdsn", "ssknd"];
var info = [];

// length Property: it display in the nodejs the total code/property in the array
// console.log(data.length); // 8
// console.log(info.length); //0

// push(): this is use to insert a new data at the end of existing data
// data.push("Kabir");
// console.log(data);

//unshift(): this is use to insert a new data at the start of existing data
// data.unshift("Sagar");
// data.unshift("Javascript");
// console.log(data);

// //pop(): this is use to remove a data starting from the end of existing data
// data.pop();
// data.pop();
// data.pop();
// console.log(data);

// //shift(): this is use to remove a data starting from the start of existing data

data.shift();
data.shift();
console.log(data);
